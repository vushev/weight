# Weight Challenge API

## Setup
need to do something on every CI from github actions

docker stop weight-challenge-api
docker rm weight-challenge-api

docker rmi weight-api
docker build -t weight-api .


### Docker

```bash
docker compose up -d
```

### Systemd

```bash
sudo systemctl daemon-reload
sudo systemctl enable weight-challenge-api
sudo systemctl start weight-challenge-api
```